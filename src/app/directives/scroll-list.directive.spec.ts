import { ScrollListDirective } from './scroll-list.directive';

describe('ScrollListDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollListDirective();
    expect(directive).toBeTruthy();
  });
});
