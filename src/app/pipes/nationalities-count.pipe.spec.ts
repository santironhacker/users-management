import { NationalitiesCountPipe } from './nationalities-count.pipe';

describe('NationalitiesCountPipe', () => {
  it('create an instance', () => {
    const pipe = new NationalitiesCountPipe();
    expect(pipe).toBeTruthy();
  });
});
