import { GoogleMapsPipe } from './google-maps.pipe';

describe('GoogleMapsPipe', () => {
  it('create an instance', () => {
    const pipe = new GoogleMapsPipe();
    expect(pipe).toBeTruthy();
  });
});
